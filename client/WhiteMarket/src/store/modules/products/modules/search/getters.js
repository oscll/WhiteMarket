/* eslint-disable */
export default {
    search(state) {
      return state.search;
    },
    latidude(state) {
      return state.latidude;
    },
    longitude(state) {
      return state.longitude;
    },
    categories(state) {
      return state.categories;
    },
    ordering(state) {
      return state.ordering;
    },
  };